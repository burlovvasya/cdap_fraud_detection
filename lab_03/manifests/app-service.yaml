apiVersion: v1
kind: Service
metadata:
  name: fraud-app-service
spec:
  selector:
    app: fraud-detection
    component: app
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30005
  type: NodePort
